#####################
Juguem amb la tortuga
#####################


Python ens ofereix moltes eines interessants per a aprendre a programar. Una
d'elles és el mòdul ``turtle`` (tortuga)

Considera el següent exemple:

.. literalinclude:: _code/turtle_square.py
    :linenos:
    :language: python

En executar-ho, el programa ens obre una finestra amb el següent contingut:

.. image:: _images/turtle_square.png
    :align: center

.. note::

   En cas que aparegui un error d'importació com ara

   .. code-block:: none

       Traceback (most recent call last):
         File "/usr/lib/python3.5/tkinter/__init__.py", line 36, in <module>
           import _tkinter
       ImportError: No module named '_tkinter'

       During handling of the above exception, another exception occurred:

       Traceback (most recent call last):
         File "_code/turtle_spiral.py", line 7, in <module>
           import turtle
         File "/usr/lib/python3.5/turtle.py", line 107, in <module>
           import tkinter as TK
         File "/usr/lib/python3.5/tkinter/__init__.py", line 38, in <module>
           raise ImportError(str(msg) + ', please install the python3-tk package')
       ImportError: No module named '_tkinter', please install the python3-tk package

   En aquest cas, cal que instal·lis el packet ``python3-tk``. Per
   exemple amb:

   .. code-block:: sh

       $ sudo apt install python3-tk

Considera ara un altre example una miqueta més sofisticat:

.. literalinclude:: _code/turtle_spiral.py
    :linenos:
    :language: python


En execurar-ho, aquest altre programa ens dibuixarà el següent:

.. image:: _images/turtle_spiral.png
    :align: center

Exercicis
=========

Practiquem una mica tot modificant els programes anteriors. Per suposat, no
esperis entendre-ho tot, simplement passa-t'ho bé!

Crea una carpeta anomenada ``00_04_tortuguejant/`` per posar-hi el codi de tots
aquests exercicis.

.. code-block:: bash

    $ mkdir 00_04_tortuguejant
    $ cd 00_04_tortuguejant

Cada cop que tinguis resolt un dels exercicis, recorda afegir-ho al control de
versions:

.. code-block:: bash

    $ git add «noufitxer»
    $ git commit -am "Completat l'exercici «…»"

Ajuda't amb ``git status`` en cas que no recordis quins canvis has realitzat.

Finalment, assegura't que puges tots els canvis a GitHub. **No cal que ho facis
per cada commit**.

Exercici Tortuga 1. Quadrat a l'inrevés
---------------------------------------

Fes una nova versió del ``turtle_square.py`` que dibuixi el quadrat cap a la dreta en comptes de
l'esquerra.

La nova versió ha d'anar al fitxer ``turtle_square_right.py``. 

Potser et resultarà còmode fer una còpia de l'original ``turtle_square.py`` i
editar-la.

.. code-block:: bash

    $ cp turtle_square.py turtle_square_right.py
    $ gedit turtle_square_right.py &

Exercici Tortuga 2. Quadrat d'un altre color
--------------------------------------------

Fes una nova versió de ``turtle_square.py`` que dibuixi el quadrat d'un altre color (ex. vermell
*red*)

El nou fitxer es dirà ``turtle_square_red.py``.

Exercici Tortuga 3. Un rombe
----------------------------

En comptes de dibuixar un quadrat, prova ara de dibuixar un rombe quadrat com el següent

.. image:: _images/turtle_rombe.png
    :align: center

El nou fitxer es dirà ``turtle_rombe.py``.

**Pista**: en comptes de girar 90 graus, pots girar altres quantitats, per exemple 45.

Exercici Tortuga 4. Espiral més grossa
--------------------------------------

Modifica el codi de ``turtle_spiral.py`` de manera que l'espiral sigui més grossa.

El nou fitxer es dirà ``turtle_big_spiral.py``.

**Pista**: amb el codi original, el costat més llarg té 100 passes (pixels).
Posa-hi, per exemple, 1000!

Exercici Tortuga 5. Quadrat multicolor
--------------------------------------

Fes que el quadrat de ``turtle_square.py`` es dibuixi de la següent manera:

.. image:: _images/turtle_square_colors.png
    :align: center

El nou fitxer es dirà ``turtle_multicolored_square.py``.

**Pista**: pots canviar el color tot just abans de fer que la tortuga camini.
Els colors de l'exemple són blau, vermell, groc (*yellow*) i verd (*green*)

Exercici Tortuga 6. Espiral pirada
----------------------------------

Canvia l'angle de gir de l'espiral de ``turtle_spiral.py`` de manera que, en comptes de 90 graus,
giri 75.

El nou fitxer es dirà ``turtle_spiral_75.py``.

Experimenta amb altres angles si et ve de gust.

Exercici Tortuga 7. Més tortugues
---------------------------------

No cal que ens quedem amb una única tortuga. Per exemple, el següent codi crea dues tortugues i les
anomena ``blava`` i ``vermella``.

.. code-block:: py

    """
        turtle_redandblue.py

        Demostració de l'ús de Turtle dibuixant dues espirals a l'hora
    """

    import turtle
    blava = turtle.Pen()
    blava.color('blue')
    vermella = turtle.Pen()         # Una altra tortuga!
    vermella.color('red')
    vermella.left(45)               # La tortuga vermella comença una mica abançada
    for passes in range(100):
        blava.right(90)
        blava.forward(passes)
        vermella.left(90)
        vermella.forward(passes)
    input()


Crea una nova versió del programa que faci servir tres tortugues. La nova pot ser de color verd i
fer els angles de 75 graus en comptes de 90 i que, camini el doble de passes que les altres dues a
cada gir.

El nou fitxer es dirà ``turtle_crowded.py``.

**Pista**: el doble de passes és ``passes * 2``

Exercici Tortuga 8. El teu repte
--------------------------------

Ara que ja tens una idea, fes un repte. És a dir, codifica un programa que faci un dibuix amb el
mòdul ``turtle``. Ensenya-li el resultat (no el codi) als companys i repta'ls per que l'intentin
aconseguir.

Anomena el fitxer ``turtle_challenge.py``.

En cas que necessitis més inspiració, et proposo que miris per la Web. Amb una
cerca ràpida d'exemples amb "python turtle", hi trobaràs entrades tan
interessants com `aquesta <https://www.michael0x2a.com/blog/turtle-examples>`_.






